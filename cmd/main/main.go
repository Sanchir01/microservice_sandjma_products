package main

import "log/slog"

var (
	development = "development"
	production  = "production"
)

func main() {
	// init config

	// init logger

	// init app golang

	// app gracefully stop
	slog.Warn("starting app")
}
